import { StyleSheet } from "react-native"

const loginStyle = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    field: {
        marginBottom: 6,
    },
    label: {
    },
    input: {
        width: 200,
        height: 40,
        padding: 10,
        borderColor: 'black',
        borderWidth: 1,
    }
})

export { loginStyle }
